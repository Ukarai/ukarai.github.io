import React from 'react'
import { Button } from '@material-ui/core'

export const ValidateRoutineButton = () => {
    return (
        <Button
            style={{ position: "relative" }}
            size="large"
            variant="contained"
            color="primary">
            Validate Routine
        </Button>
    )
}